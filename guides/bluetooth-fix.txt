sudo pacman -S pulseaudio pulseaudio-bluetooth

pulseaudio -k
pulseaudio --start

sudo systemctl restart bluetooth

bluetoothctl remove $MACA
bluetoothctl scan on
bluetoothctl pair $MACA
bluetoothctl trust $MACA
bluetoothctl connect $MACA

pactl list modules | grep bluetooth

---------------------------------------------------------

sudo nano /etc/pulse/default.pa

(CTRL+F load-module module-bluetooth-discover)

load-module module-bluetooth-discover avrcp_absolute_volume=false
