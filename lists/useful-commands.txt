#know when was a package installed
pacman -Qi <package_name> | grep "Install Date"

#confirm if my host is showing headers in ports
curl -v http://localhost 2>&1 | grep -i "< Server"

#know open ports
sudo ss -tulpn

#nmap
sudo nmap -p- --open -sS -vvv -Pn -n (domain)
sudo nmap -sC -sV -p(ports discovered) (domain)

#whatweb
whatweb (domain)

#create a file with the differences between 2 files
diff file1 file2 > newfile

#fail2ban
sudo fail2ban-client status (rule)
sudo nano /var/log/fail2ban.log

sudo journalctl -u (service).service -f
sudo fail2ban-regex systemd-journal (service).service

sudo fail2ban-regex path/to/log /etc/fail2ban/filter.d/(rule).conf

sudo fail2ban-client set (rule) ban/unban (<ADDR>)

#chafa for fastfetch
chafa --symbols=braille --colors none --fg ff0000 -s 50x50 path/to/image

#solution when i right click and it sends me to the previous page, in firefox
about:config (in firefox)
ui.context_menus.after_mouseup
set as "true"

#enable userChrome.css in firefox
about:config (in firefox)
toolkit.legacyUserProfileCustomizations.stylesheets
set as "true"

about:support
"Profile Directory" > "Open Directory"
mkdir ~/.mozilla/firefox/(profile)/chrome
touch userChrome.css

#togle line numbers in nano
Alt + N
